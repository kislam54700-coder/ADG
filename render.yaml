```yaml
services:
  - type: web
    name: adg-anime-draft-game
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
```
